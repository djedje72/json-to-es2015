get $$key$$() {
        return this[$$key$$];
    }
    set $$key$$(_values) {
        this[$$key$$] = _values instanceof Array ? _values.map((_value) => Object.assign(new $$upperKey$$Value(), _value)) : [];
    }