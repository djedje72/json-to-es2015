get $$key$$() {
        return this[$$key$$];
    }
    set $$key$$(_values) {
        this[$$key$$] = _values instanceof Array ? _values.map(_value => _.merge(new $$upperKey$$(), _value)) : [];
    }